import {type Document as MediaDocument, type Meme} from '#/lib/mock-data/types'

export type Mode = 'Memes' | 'Documents'
export type ViewStyleMode = 'board' | 'deck'
export type MediaItem = Meme | MediaDocument
